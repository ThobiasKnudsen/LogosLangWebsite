# a function can return a type; the call runs at
# parse time and becomes the type it yields
pick := fn (i := i32 ?) -> type (
    if (i == 0) (i32) else (f64)
),

mut a := pick(1) ?,   # a is an f64 place
a = 9.9,
print «{pick(0) == i32}»   # true
