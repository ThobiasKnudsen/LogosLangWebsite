# one word for both: an unmarked member is a place per
# instance, a share one is one place, read both ways
point := type (
    x := i32 ?,
    y := i32 ?,
    share dims := i32 2
),
p := point (1, 2),
print «{p.x} {point.dims} {p.dims}»   # 1 2 2
